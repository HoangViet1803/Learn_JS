/** indexOf(): takes a substring and if the substring exists in string it returns the first position of the substring if does not exist it returns -1 */
// string.indexOf(substring)
let myString = 'Life is a Story';
console.log(myString.indexOf(i)); // 2
console.log(myString.indexOf(w)); // -1